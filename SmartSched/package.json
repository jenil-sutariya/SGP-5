{
  "name": "smartsched",
  "version": "1.0.0",
  "private": true,
  "description": "SmartSched – Automated University Timetable Generation System",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend --if-present",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run prisma:generate --prefix backend"
  },
  "engines": {
    "node": ">=20"
  }
}
